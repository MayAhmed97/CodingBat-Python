def string_match(a, b):
  shorter = min(len(a), len(b))
  Result = 0
  n = len(a)
  m = len(b)
  for i in range(shorter):
    if len(a[i:i+2])<2 : return Result #KARIM
    if a[i:i+2] == b[i:i+2]: 
       Result += 1
  return Result
